# Contributing

感谢你改进 Veang Workbench。

1. 从 `main` 创建主题分支。
2. 保持改动聚焦，不要提交项目文件、会话、凭据或构建产物。
3. 运行 `pnpm test`。
4. 在 Pull Request 中说明用户可见变化、验证方式和兼容的 DSH 版本。

所有贡献必须由贡献者合法提交，并以本项目的 MIT License 发布。引入第三方代码时，请同时补充来源、版权和许可证信息。

## 新增文件访问接口时

工作区边界有两个校验函数，**新代码一律使用 `realTargetFrom()`**：

- `targetFrom()` 只比较路径字符串，看不穿符号链接，仅用于规范化根目录。
- `realTargetFrom()` 在此基础上解析真实路径，是唯一可用于访问文件系统的入口。

用错会重新打开工作区逃逸漏洞（见 CHANGELOG）。`tests/security.test.mjs` 中的测试会检查所有调用点，用错时 CI 会直接失败并指出具体行号。

## 报告安全问题

请不要通过公开 Issue 或 Pull Request 提交尚未修复的安全问题。工作台具备工作区内的文件读写与命令执行能力，任何绕过工作区边界、任意路径访问、未授权上传或内容注入的问题都应按安全漏洞处理，报告方式见 [SECURITY.md](./SECURITY.md)。
