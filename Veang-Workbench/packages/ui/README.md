# veang-workbench-ui

Internal workbench surface for [Veang Workbench](../../README.md). Install the `veang-workbench` bundle instead of this package directly.

In Veang Workbench `0.1.1`, this source is embedded into the main package as `veang-workbench/ui` during release and is not published separately.
