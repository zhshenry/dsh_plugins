# 0.2.0: the stock shell stays enabled (additive form, worktable-style).
# The workbench UI plugin renders into the stock shell.overlay seat and
# squeezes the stock conversation to the right edge; no official plugin is
# replaced or disabled.
- insert:
    - id: veang-ui
      name: veang-workbench
